# Servicio: citas

Servidor Fastify + Swagger ya configurado. Implementa la lógica en `src/routes.ts`.

- Swagger UI: `/docs`  ·  Health: `/health`
- Esquema(s) de BD: ver `infra/postgres/init/`
- Patrón de referencia: `apps/services/auth` y `apps/services/pacientes`
- Dev: `npm run dev -w apps/services/citas`

Detalle de tareas en `docs/DIVISION_MODULOS.md`.
